using System.Windows;

namespace Dzl.Tray;

/// <summary>
/// Minimal placeholder main window (Task 4.4 builds it out). Openable from the tray.
/// </summary>
public partial class MainWindow : Window
{
    public MainWindow()
    {
        InitializeComponent();
    }
}
