<Application x:Class="Dzl.Tray.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Dzl.Tray"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Wpf.Ui Fluent control + theme dictionaries (dark applied at startup in App.xaml.cs). -->
                <ui:ThemesDictionary Theme="Dark" />
                <ui:ControlsDictionary />
            </ResourceDictionary.MergedDictionaries>

            <!-- Shared converters + dark DataGrid styles (app scope so windows AND
                 UserControls resolve them - UserControls never see host-window resources). -->
            <BooleanToVisibilityConverter x:Key="BoolToVis" />
            <local:InverseBoolToVisibilityConverter x:Key="InverseBoolToVis" />
            <!-- One bool->brush converter, two configured instances (app scope so UserControls resolve
                 them too). FlagBrush = CE flag glyphs (active green / muted); StatusDotConverter = the
                 SERVER/CLIENT/P:/GitHub/Steam status dots (accent-green up / muted-grey down). -->
            <local:BoolToBrushConverter x:Key="FlagBrush">
                <local:BoolToBrushConverter.True>
                    <SolidColorBrush Color="#7CE3A1" />
                </local:BoolToBrushConverter.True>
                <local:BoolToBrushConverter.False>
                    <SolidColorBrush Color="#555B63" />
                </local:BoolToBrushConverter.False>
            </local:BoolToBrushConverter>
            <local:BoolToBrushConverter x:Key="StatusDotConverter">
                <local:BoolToBrushConverter.True>
                    <SolidColorBrush Color="#4CAF50" />
                </local:BoolToBrushConverter.True>
                <local:BoolToBrushConverter.False>
                    <SolidColorBrush Color="#6E6E6E" />
                </local:BoolToBrushConverter.False>
            </local:BoolToBrushConverter>
            <!-- Dark Fluent-friendly DataGrid style: the stock WPF DataGrid renders light/grey,
           so we restyle row height, gridlines, header and selection to match the theme. -->
            <Style x:Key="DarkDataGrid" TargetType="DataGrid">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="RowBackground" Value="Transparent" />
                    <Setter Property="AlternatingRowBackground" Value="Transparent" />
                    <Setter Property="GridLinesVisibility" Value="Horizontal" />
                    <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource ControlElevationBorderBrush}" />
                    <Setter Property="RowHeight" Value="32" />
                    <Setter Property="ColumnHeaderHeight" Value="34" />
                    <Setter Property="CanUserResizeRows" Value="False" />
            </Style>

            <Style x:Key="DarkDataGridColumnHeader" TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="{DynamicResource ControlFillColorDefaultBrush}" />
                    <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                    <Setter Property="FontSize" Value="12" />
                    <Setter Property="Padding" Value="10,0" />
                    <Setter Property="VerticalContentAlignment" Value="Center" />
                    <Setter Property="BorderThickness" Value="0,0,0,1" />
                    <Setter Property="BorderBrush" Value="{DynamicResource ControlElevationBorderBrush}" />
            </Style>

            <Style x:Key="DarkDataGridRow" TargetType="DataGridRow">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
              <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="{DynamicResource SystemAccentColorSecondaryBrush}" />
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}" />
                </Trigger>
              </Style.Triggers>
            </Style>

            <Style x:Key="DarkDataGridCell" TargetType="DataGridCell">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="VerticalContentAlignment" Value="Center" />
                    <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
              <!-- Default cell padding; bound via the template so a per-column CellStyle can override it
             (e.g. the checkbox column sets Padding=0 so the box isn't clipped). -->
                    <Setter Property="Padding" Value="8,0" />
                    <Setter Property="Template">
                      <Setter.Value>
                  <ControlTemplate TargetType="DataGridCell">
                    <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                      <ContentPresenter VerticalAlignment="Center" />
                    </Border>
                  </ControlTemplate>
                </Setter.Value>
              </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
